server {
    listen 80;
    server_name localhost;

    location /33zzadmin/ {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /33zzadmin/index.html;
    }
}
